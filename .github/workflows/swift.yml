name: Swift

on:
  push:
    branches: 
      - master
  pull_request:
    branches: 
      - master
      - vapor4.0

jobs:
  build:

    runs-on: macos-10.15

    steps:
    - uses: actions/checkout@v2
    - name: Build
      run: swift build

